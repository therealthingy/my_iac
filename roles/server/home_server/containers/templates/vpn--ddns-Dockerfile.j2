{{ ansible_managed|comment }}
FROM ubuntu:22.04

RUN apt update  &&  apt install -y python3-pip \
 && pip install --no-cache-dir netifaces requests

WORKDIR /usr/local/bin
COPY ddns-update .

CMD ["python3", "-u", "ddns-update"]
