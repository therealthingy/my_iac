---
# !!  KEEP IN MIND: Must match Hosts in `~/.ssh/config` (auth details will be taken from this file)  !!
all:
  children:
    docker:
      hosts:
#        rpi:
        linux-dev-vm.local:

    workstations:
      hosts:
        linux-dev-vm.local:

    servers:
      hosts:
#        rpi:
        linux-dev-vm.local:

    homeservers:
      hosts:
#        rpi:
        linux-dev-vm.local:


#    rpis:
#      hosts:
#        rpi:

    vmware_vms:
      hosts:
        linux-dev-vm.local:
