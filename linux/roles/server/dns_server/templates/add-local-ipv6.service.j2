[Unit]
Description=thlu:{{ ipv6_systemd_service_name }}      Add a local static IPv6 (ULA) to Network-Interface
After=basic.target

[Service]
Type=oneshot
RemainAfterExit=no
ExecStart=/usr/sbin/ip addr add {{ dns_ipv6_ula_addr }} dev {{ dns_iface }}

[Install]
WantedBy=multi-user.target
